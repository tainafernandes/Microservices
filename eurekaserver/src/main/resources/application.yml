spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: ${USERNAME}
      password: ${PASSWORD}

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
